# `dtfls` Bugs

```bash
# This does not work:
dtfls add ~/.config/shell
"Empty directory, nothing to add: /home/wrnk/.config/shell"
```


I need to make sure that `dtfls` iterates all files contained within a dir.
