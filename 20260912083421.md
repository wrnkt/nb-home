# Favorite CLI Utilities

## Overview
1. `git-delta` :: great diff coloring & huge theme selection
1. `ripgrep` :: very useful for searching
1. `fzf` :: great fuzzy finder
1. `tokei` :: count lines of code and their distribution
1. `alacritty` :: one of the best consoles, great color support & config
1. `w3m` :: cool simple terminal browser
1. `i3` :: best window manager, productivity increase is huge
1. `tmux` :: best shell manager
1. `neovim` :: no need to explain
1. `fastfetch` :: quick, pretty, & easy system info
1. `bat` :: `cat` but better, with better colors
1. `lsd` :: better `ls`, great colors, readable
1. `eza` ::
1. `exa` ::
1. `xev` :: useful for debugging. Every X event (moved, resized, clicked in, etc.) is sent to your window or a new one
1. `glow` :: provides pretty Markdown browsing & parsing within the shell

### Midight Commander

A visual, TUI, dual-panel file manager.
Uses `ncurses` or `S-lang`.

**Installation:**
```bash
# install like so:
sudo dnf install mc
sudo apt install mc
```


### Delta
A colorful, clean diff viewer. 
I have `core.pager = delta` in my `~/.gitconfig`.

** Fedora / Red Hat installation:**
```bash
sudo dnf install git-delta

# Ubuntu
# Thist installation process is a bit clunky. There must be a simpler way.
# Fetch the latest version number
DELTA_VERSION=$(curl -sL "https://api.github.com/repos/dandavison/delta/releases/latest" | awk -F\" '/"tag_name":/{print $(NF-1)}')

# Download the .deb package to your Downloads directory
mkdir -p ~/Downloads && cd ~/Downloads
curl -OL "https://github.com/dandavison/delta/releases/download/${DELTA_VERSION}/git-delta_${DELTA_VERSION}_amd64.deb"

# Install the package
sudo apt install ./git-delta_${DELTA_VERSION}_amd64.deb
cd -
```


**Configuration:**

**Command line configuration:**
```bash
git config --global core.pager "delta"
git config --global interactive.diffFilter "delta --color-only"
git config --global delta.navigate true
git config --global merge.conflictStyle zdiff3
```



**Configuration through `.gitconfig`:**
```yaml
[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # Use 'n' and 'N' to jump between file diff sections
    side-by-side = true # Optional: Enables side-by-side view instead of stacked

[merge]
    conflictStyle = zdiff3
```



### ripgrep

### nb
Version controlled notes and scratch framework that works across multiple devices.
